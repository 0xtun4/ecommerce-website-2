kind: Service                             
apiVersion: v1
metadata:
  name: phpweb-service
  labels:
    app: phpweb-service-label
spec:
  ports:
    - port: 8080                               
      targetPort: 80                    
      nodePort: 31200
  externalIPs:
    - 52.221.241.81
  selector:
    app: phpweb                  
  type: LoadBalancer                   